{
  "$schema": "http://json-schema.org/schema",
  "$id": "QmlComponentSchema",
  "title": "QML Component Options",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Component name"
    },
    "qmlFile": {
      "type": "string",
      "description": "Path to source QML file"
    },
    "path": {
      "type": "string",
      "description": "Legacy destination path override maintained for backward compatibility",
      "deprecated": true
    },
    "project": {
      "type": "string",
      "description": "Angular workspace project to generate into"
    },
    "feature": {
      "type": "string",
      "description": "Feature folder under the target project's app root"
    },
    "updateBarrel": {
      "type": "boolean",
      "description": "Update the feature barrel file with the generated component export",
      "default": false
    },
    "routeMode": {
      "type": "string",
      "description": "Route insertion strategy for standalone component generation",
      "enum": ["none", "project", "feature"],
      "default": "none"
    }
  },
  "required": ["name", "qmlFile"]
}
