{
  "$schema": "http://json-schema.org/schema",
  "$id": "QmlBatchSchema",
  "title": "QML Batch Component Generator Options",
  "type": "object",
  "properties": {
    "qmlDir": {
      "type": "string",
      "description": "Path to directory containing QML files"
    },
    "path": {
      "type": "string",
      "description": "Legacy destination path override for generated components"
    },
    "recursive": {
      "type": "boolean",
      "description": "Recursively process subdirectories",
      "default": true
    },
    "project": {
      "type": "string",
      "description": "Angular workspace project to generate into"
    },
    "feature": {
      "type": "string",
      "description": "Feature folder under the target project's app root"
    },
    "updateBarrel": {
      "type": "boolean",
      "description": "Update the feature barrel file with generated component exports",
      "default": false
    },
    "routeMode": {
      "type": "string",
      "description": "Route insertion strategy for standalone component generation",
      "enum": ["none", "project", "feature"],
      "default": "none"
    }
  },
  "required": ["qmlDir"]
}
